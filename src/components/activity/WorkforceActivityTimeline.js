import TeamActivityTimeline from "@/components/activity/TeamActivityTimeline";

export default function WorkforceActivityTimeline({ sessions }) {
  return <TeamActivityTimeline sessions={sessions} />;
}
