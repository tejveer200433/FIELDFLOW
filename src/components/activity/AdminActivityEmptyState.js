import ActivityEmptyState from "@/components/activity/ActivityEmptyState";

export default function AdminActivityEmptyState({ title, description }) {
  return <ActivityEmptyState title={title} description={description} />;
}
