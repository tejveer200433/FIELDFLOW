"use client";
import { useCallback,useEffect,useMemo,useState } from "react";
import { ArrowLeft,CalendarDays,CheckCircle2,ChevronRight,Clock3,ExternalLink,FileText,FolderKanban,MapPin,Plus,UserRoundCheck,UsersRound,X } from "lucide-react";
import { apiJson } from "@/lib/apiClient";

const projectStatuses=["Planning","Active","On Hold","Completed","Cancelled"];
function Badge({value}){const tone=value==="Completed"||value==="Approved"?"bg-emerald-50 text-emerald-700 border-emerald-200":value==="Needs Changes"||value==="Urgent"?"bg-rose-50 text-rose-700 border-rose-200":value==="Submitted for Review"||value==="High"?"bg-amber-50 text-amber-700 border-amber-200":"bg-blue-50 text-blue-700 border-blue-200";return <span className={`inline-flex rounded-full border px-2.5 py-1 text-xs font-bold ${tone}`}>{value}</span>}
function Modal({title,onClose,wide=false,children}){return <div className="fixed inset-0 z-[1000] grid place-items-center overflow-y-auto bg-slate-950/55 p-4" onMouseDown={event=>event.target===event.currentTarget&&onClose()}><section className={`my-6 max-h-[92vh] w-full overflow-y-auto rounded-3xl bg-white p-6 shadow-2xl ${wide?"max-w-4xl":"max-w-xl"}`}><div className="flex items-center justify-between"><h2 className="text-xl font-bold">{title}</h2><button onClick={onClose} className="icon-button"><X/></button></div><div className="mt-5">{children}</div></section></div>}
const Field=({label,children})=><label className="block"><span className="label font-bold">{label}</span>{children}</label>;

export default function ProjectManagement(){
  const[projects,setProjects]=useState([]),[people,setPeople]=useState([]),[selectedId,setSelectedId]=useState(null),[projectModal,setProjectModal]=useState(false),[moduleModal,setModuleModal]=useState(false),[moduleTarget,setModuleTarget]=useState(null),[busy,setBusy]=useState(false),[message,setMessage]=useState(""),[error,setError]=useState("");
  const load=useCallback(async()=>{try{const[p,e]=await Promise.all([apiJson("/api/projects",{cache:"no-store"}),apiJson("/api/employees",{cache:"no-store"})]);setProjects(p.data);setPeople(e.data.filter(item=>item.active&&item.approvalStatus==="approved"));setError("");}catch(failure){setError(failure.message);}},[]);
  useEffect(()=>{load();},[load]);
  const selected=projects.find(item=>item.id===selectedId)||null,employees=people.filter(item=>item.role==="employee"),reviewers=people.filter(item=>["manager","admin"].includes(item.role));
  const totals=useMemo(()=>({active:projects.filter(item=>item.status==="Active").length,due:projects.filter(item=>item.deadline&&new Date(item.deadline)<new Date()&&item.status!=="Completed").length,review:projects.flatMap(p=>p.modules).flatMap(m=>m.assignments).filter(a=>a.status==="Submitted for Review").length}),[projects]);
  async function createProject(event){event.preventDefault();setBusy(true);setError("");try{const data=Object.fromEntries(new FormData(event.currentTarget));const payload=await apiJson("/api/projects",{method:"POST",body:JSON.stringify(data)});setProjectModal(false);setSelectedId(payload.data.id);setModuleTarget(null);setModuleModal(true);await load();setMessage("Project created. Now divide it into modules and assignments.");}catch(failure){setError(failure.message);}finally{setBusy(false);}}
  async function createModuleAssignment(event){event.preventDefault();setBusy(true);setError("");try{const form=Object.fromEntries(new FormData(event.currentTarget));let moduleId=moduleTarget?.id;if(!moduleId){const modulePayload=await apiJson("/api/modules",{method:"POST",body:JSON.stringify({projectId:selectedId,title:form.moduleTitle,description:form.moduleDescription,checklist:form.checklist})});moduleId=modulePayload.data.id;}await apiJson("/api/assignments",{method:"POST",body:JSON.stringify({moduleId,employeeId:form.employeeId,reviewerId:form.reviewerId,startDate:form.startDate,deadline:new Date(form.deadline).toISOString(),priority:form.priority,notes:form.notes})});setModuleModal(false);setModuleTarget(null);setMessage("Module assignment saved and visible to the selected employee.");await load();}catch(failure){setError(failure.message);}finally{setBusy(false);}}
  async function updateProject(status){const payload=await apiJson("/api/projects",{method:"PATCH",body:JSON.stringify({id:selected.id,status})});setProjects(current=>current.map(item=>item.id===selected.id?payload.data:item));}
  async function review(submission,decision,comment){setBusy(true);try{await apiJson("/api/submissions",{method:"PATCH",body:JSON.stringify({id:submission.id,decision,comment})});setMessage(decision==="Approved"?"Work approved and marked completed.":"Changes requested from employee.");await load();}catch(failure){setError(failure.message);}finally{setBusy(false);}}
  async function openFile(file){try{const payload=await apiJson(`/api/files?path=${encodeURIComponent(file.path)}`);window.open(payload.data.url,"_blank","noopener,noreferrer");}catch(failure){setError(failure.message);}}

  if(selected)return <><button onClick={()=>setSelectedId(null)} className="mb-5 inline-flex items-center gap-2 font-bold text-blue-600"><ArrowLeft className="h-4 w-4"/>All projects</button><div className="mb-7 flex flex-wrap items-start justify-between gap-4"><div><div className="flex flex-wrap gap-2"><Badge value={selected.status}/><Badge value={selected.priority}/><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-bold">{selected.category}</span></div><h1 className="mt-3 text-3xl font-extrabold sm:text-4xl">{selected.title}</h1><p className="mt-2 max-w-3xl text-slate-500">{selected.description||"No project description."}</p></div><div className="flex gap-2"><select value={selected.status} onChange={e=>updateProject(e.target.value)} className="input w-auto">{projectStatuses.map(item=><option key={item}>{item}</option>)}</select><button onClick={()=>{setModuleTarget(null);setModuleModal(true)}} className="btn-primary"><Plus/>Add module</button></div></div>{message&&<p className="mb-5 rounded-2xl bg-emerald-50 p-4 text-emerald-700">{message}</p>}{error&&<p className="mb-5 rounded-2xl bg-rose-50 p-4 text-rose-700">{error}</p>}<div className="grid gap-4 sm:grid-cols-4"><div className="card p-5"><p className="text-xs uppercase text-slate-500">Progress</p><strong className="text-2xl text-blue-700">{selected.progress}%</strong></div><div className="card p-5"><p className="text-xs uppercase text-slate-500">Modules</p><strong className="text-2xl">{selected.modules.length}</strong></div><div className="card p-5"><p className="text-xs uppercase text-slate-500">Start date</p><strong>{selected.startDate||"Not set"}</strong></div><div className="card p-5"><p className="text-xs uppercase text-slate-500">Deadline</p><strong>{selected.deadline||"Not set"}</strong></div></div><section className="card mt-6 p-6"><div className="grid gap-4 text-sm sm:grid-cols-2 lg:grid-cols-4"><div><p className="text-slate-500">Client</p><strong>{selected.clientCompany||"—"}</strong></div><div><p className="text-slate-500">Contact</p><strong>{selected.contactPerson||"—"} {selected.contactPhone&&`· ${selected.contactPhone}`}</strong></div><div><p className="text-slate-500">Project owner</p><strong>{selected.owner}</strong></div><div><p className="text-slate-500">Site</p><strong>{selected.siteAddress||"—"}</strong></div></div>{selected.expectedOutcome&&<div className="mt-5 rounded-2xl bg-blue-50 p-4"><p className="text-xs font-bold uppercase text-blue-600">Expected outcome</p><p className="mt-1">{selected.expectedOutcome}</p></div>}</section><div className="mt-7 space-y-5">{selected.modules.map(module=><section key={module.id} className="card overflow-hidden"><div className="flex flex-wrap items-start justify-between gap-3 border-b bg-slate-50 p-5"><div><h2 className="text-xl font-bold">{module.title}</h2><p className="mt-1 text-sm text-slate-500">{module.description||"No module description."}</p></div><button onClick={()=>{setModuleTarget(module);setModuleModal(true)}} className="btn-secondary"><UsersRound className="h-4 w-4"/>Assign teammate</button></div>{module.checklist.length>0&&<div className="border-b px-5 py-4"><p className="text-xs font-bold uppercase tracking-widest text-slate-500">Checklist</p><div className="mt-2 flex flex-wrap gap-2">{module.checklist.map(item=><span key={item} className="rounded-full bg-slate-100 px-3 py-1 text-xs">{item}</span>)}</div></div>}<div className="divide-y">{module.assignments.map(assignment=>{const latest=assignment.submissions[0];return <div key={assignment.id} className="p-5"><div className="grid gap-4 lg:grid-cols-[1.2fr_1fr_1fr_auto]"><div><p className="font-bold">{assignment.employee}</p><p className="text-xs text-slate-500">Reviewed by {assignment.reviewer}</p></div><div><p className="text-xs text-slate-500">Schedule</p><p className="text-sm font-semibold">{assignment.startDate} → {new Date(assignment.deadline).toLocaleString()}</p></div><div><Badge value={assignment.status}/><p className="mt-2 text-xs text-slate-500">Priority: {assignment.priority}</p></div><div className="text-right"><span className="text-sm font-bold text-blue-700">{assignment.checklistProgress.length}/{module.checklist.length} checks</span></div></div>{latest&&<div className="mt-4 rounded-2xl border border-blue-100 bg-blue-50/60 p-4"><div className="flex flex-wrap justify-between gap-2"><strong>Submission v{latest.version}</strong><Badge value={latest.workStatus}/></div><p className="mt-2 text-sm">{latest.summary}</p>{latest.externalLink&&<a href={latest.externalLink} target="_blank" rel="noreferrer" className="mt-2 inline-flex items-center gap-1 text-sm font-bold text-blue-600"><ExternalLink className="h-4 w-4"/>Work link</a>}<div className="mt-3 flex flex-wrap gap-2">{latest.files.map(file=><button key={file.id} onClick={()=>openFile(file)} className="btn-secondary py-2 text-xs"><FileText className="h-4 w-4"/>{file.name}</button>)}</div>{latest.workStatus==="Submitted for Review"&&<ReviewControls busy={busy} onReview={(decision,comment)=>review(latest,decision,comment)}/>} {latest.reviewerComment&&<p className="mt-3 rounded-xl bg-white p-3 text-sm"><strong>Reviewer:</strong> {latest.reviewerComment}</p>}</div>}</div>})}{!module.assignments.length&&<p className="p-5 text-sm text-slate-500">No employee assigned yet.</p>}</div></section>)}{!selected.modules.length&&<div className="card p-12 text-center"><FolderKanban className="mx-auto h-10 w-10 text-blue-500"/><h2 className="mt-3 font-bold">Create the first module</h2><p className="mt-1 text-sm text-slate-500">Divide this project into frontend, backend, testing, field work, or any modules you need.</p></div>}</div>{moduleModal&&<ModuleForm module={moduleTarget} employees={employees} reviewers={reviewers} busy={busy} onClose={()=>{setModuleModal(false);setModuleTarget(null)}} onSubmit={createModuleAssignment}/>}</>;

  return <><div className="mb-7 flex flex-wrap items-end justify-between gap-4"><div><h1 className="text-3xl font-extrabold sm:text-4xl">Projects & work orders</h1><p className="mt-2 text-slate-500">Create a project, divide it into modules, and assign each part to your team.</p></div><button onClick={()=>setProjectModal(true)} className="btn-primary rounded-full px-7 py-4"><Plus/>New project</button></div>{message&&<p className="mb-5 rounded-2xl bg-emerald-50 p-4 text-emerald-700">{message}</p>}{error&&<p className="mb-5 rounded-2xl bg-rose-50 p-4 text-rose-700">{error}</p>}<div className="grid gap-4 sm:grid-cols-3"><div className="card p-5"><p className="text-xs uppercase text-slate-500">Active projects</p><strong className="text-3xl text-blue-700">{totals.active}</strong></div><div className="card p-5"><p className="text-xs uppercase text-slate-500">Awaiting review</p><strong className="text-3xl text-amber-600">{totals.review}</strong></div><div className="card p-5"><p className="text-xs uppercase text-slate-500">Past deadline</p><strong className="text-3xl text-rose-600">{totals.due}</strong></div></div><div className="mt-6 grid gap-5 md:grid-cols-2 xl:grid-cols-3">{projects.map(project=><button key={project.id} onClick={()=>setSelectedId(project.id)} className="card p-6 text-left transition hover:-translate-y-0.5 hover:border-blue-200 hover:shadow-lg"><div className="flex justify-between gap-3"><Badge value={project.status}/><Badge value={project.priority}/></div><h2 className="mt-4 text-xl font-bold">{project.title}</h2><p className="mt-1 text-sm text-slate-500">{project.clientCompany||project.category}</p><div className="mt-5 h-2 overflow-hidden rounded-full bg-slate-100"><span className="block h-full bg-blue-600" style={{width:`${project.progress}%`}}/></div><div className="mt-2 flex justify-between text-xs text-slate-500"><span>{project.progress}% complete</span><span>{project.modules.length} modules</span></div><div className="mt-5 flex items-center justify-between border-t pt-4 text-sm"><span className="flex items-center gap-1 text-slate-500"><CalendarDays className="h-4 w-4"/>{project.deadline||"No deadline"}</span><ChevronRight className="text-blue-600"/></div></button>)}{!projects.length&&<div className="card col-span-full p-14 text-center"><FolderKanban className="mx-auto h-12 w-12 text-blue-500"/><h2 className="mt-4 text-xl font-bold">No projects yet</h2><p className="mt-2 text-slate-500">Create your first project, then divide it among your team.</p></div>}</div>{projectModal&&<ProjectForm busy={busy} onClose={()=>setProjectModal(false)} onSubmit={createProject}/>}</>;
}

function ProjectForm({busy,onClose,onSubmit}){return <Modal title="Create project" onClose={onClose} wide><form onSubmit={onSubmit} className="space-y-5"><div className="rounded-2xl bg-blue-50 p-4"><p className="text-xs font-bold uppercase tracking-widest text-blue-600">Step 1 · Project identity</p><p className="mt-1 text-sm text-blue-800">Start with the project name. After saving, you will divide it into modules and employees.</p></div><Field label="Project title / name"><input name="title" required autoFocus className="input" placeholder="Example: FieldFlow Mobile App"/></Field><div className="grid gap-4 sm:grid-cols-2"><Field label="Client / company"><input name="clientCompany" className="input"/></Field><Field label="Work category"><select name="category" className="input"><option>Software</option><option>Visit</option><option>Installation</option><option>Service</option><option>Sales</option><option>Collection</option><option>Other</option></select></Field><Field label="Contact person"><input name="contactPerson" className="input"/></Field><Field label="Contact phone"><input name="contactPhone" className="input"/></Field></div><Field label="Site address"><input name="siteAddress" className="input" placeholder="Address or remote"/></Field><div className="grid gap-4 sm:grid-cols-2"><Field label="Map latitude"><input name="siteLat" type="number" step="any" className="input"/></Field><Field label="Map longitude"><input name="siteLng" type="number" step="any" className="input"/></Field></div><Field label="Project description"><textarea name="description" className="input min-h-24"/></Field><Field label="Expected outcome"><textarea name="expectedOutcome" className="input min-h-20"/></Field><div className="grid gap-4 sm:grid-cols-3"><Field label="Start date"><input name="startDate" type="date" className="input"/></Field><Field label="Deadline"><input name="deadline" type="date" className="input"/></Field><Field label="Priority"><select name="priority" defaultValue="Medium" className="input"><option>Low</option><option>Medium</option><option>High</option><option>Urgent</option></select></Field></div><button disabled={busy} className="btn-primary w-full py-4">{busy?"Creating…":"Create project & add modules"}</button></form></Modal>}
function ModuleForm({module,employees,reviewers,busy,onClose,onSubmit}){return <Modal title={module?`Assign teammate · ${module.title}`:"Create module & assign work"} onClose={onClose} wide><form onSubmit={onSubmit} className="space-y-5">{!module&&<><div className="rounded-2xl bg-violet-50 p-4"><p className="text-xs font-bold uppercase tracking-widest text-violet-600">Step 2 · Divide the project</p><p className="mt-1 text-sm text-violet-800">Create a module such as Frontend, Backend, Design, Testing, or Deployment.</p></div><Field label="Module / work title"><input name="moduleTitle" required className="input" placeholder="Example: Frontend dashboard"/></Field><Field label="Work description"><textarea name="moduleDescription" className="input min-h-24" placeholder="Explain exactly what should be completed."/></Field><Field label="Checklist (one item per line)"><textarea name="checklist" className="input min-h-28" placeholder={'Create responsive screens\nConnect project API\nTest mobile layout'}/></Field></>}<div className="grid gap-4 sm:grid-cols-2"><Field label="Assign employee"><select name="employeeId" required className="input"><option value="">Choose employee</option>{employees.map(item=><option key={item.id} value={item.id}>{item.name}</option>)}</select></Field><Field label="Reviewer"><select name="reviewerId" required className="input"><option value="">Choose manager/admin</option>{reviewers.map(item=><option key={item.id} value={item.id}>{item.name} · {item.role}</option>)}</select></Field><Field label="Employee start date"><input name="startDate" required type="date" className="input"/></Field><Field label="Deadline and time"><input name="deadline" required type="datetime-local" className="input"/></Field><Field label="Priority"><select name="priority" className="input"><option>Low</option><option>Medium</option><option>High</option><option>Urgent</option></select></Field></div><Field label="Notes for employee"><textarea name="notes" className="input min-h-20" placeholder="Access details, standards, dependencies, or instructions."/></Field>{!employees.length&&<p className="rounded-xl bg-amber-50 p-3 text-sm text-amber-700">No approved employee accounts are available. Create or approve an employee first.</p>}<button disabled={busy||!employees.length||!reviewers.length} className="btn-primary w-full py-4">{busy?"Saving…":module?"Assign teammate":"Create module & assignment"}</button></form></Modal>}
function ReviewControls({busy,onReview}){const[comment,setComment]=useState("");return <div className="mt-4 border-t border-blue-100 pt-4"><textarea value={comment} onChange={e=>setComment(e.target.value)} className="input min-h-20 bg-white" placeholder="Reviewer feedback"/><div className="mt-3 flex flex-wrap gap-2"><button disabled={busy} onClick={()=>onReview("Approved",comment)} className="btn-primary"><CheckCircle2 className="h-4 w-4"/>Approve work</button><button disabled={busy} onClick={()=>onReview("Needs Changes",comment)} className="btn-secondary">Request changes</button></div></div>}
