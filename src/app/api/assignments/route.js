import { ApiError,apiFailure,requireSession } from "@/lib/supabaseServer";
import { mapAssignment } from "@/lib/projectData";
const assignmentSelect=`*,employee:profiles!work_assignments_employee_id_fkey(full_name,email),reviewer:profiles!work_assignments_reviewer_id_fkey(full_name,email),work_submissions(*,submission_files(*))`;
const statuses=["Not Started","In Progress","Submitted for Review","Needs Changes","Completed"];
export async function POST(request){try{const{client,profile}=await requireSession(request,["manager","admin"]);const body=await request.json();if(!body.moduleId||!body.employeeId||!body.reviewerId||!body.startDate||!body.deadline)throw new ApiError("Module, employee, reviewer, start date and deadline are required.");const{data:people,error:peopleError}=await client.from("profiles").select("id,role,active,approval_status").in("id",[body.employeeId,body.reviewerId]);if(peopleError)throw peopleError;const employee=people.find(item=>item.id===body.employeeId),reviewer=people.find(item=>item.id===body.reviewerId);if(!employee||employee.role!=="employee"||!employee.active||employee.approval_status!=="approved")throw new ApiError("Choose an approved employee.");if(!reviewer||!["manager","admin"].includes(reviewer.role)||!reviewer.active)throw new ApiError("Choose an active manager or administrator as reviewer.");const{data,error}=await client.from("work_assignments").insert({module_id:body.moduleId,employee_id:body.employeeId,reviewer_id:body.reviewerId,start_date:body.startDate,deadline:body.deadline,priority:body.priority||"Medium",employee_notes:String(body.notes||"").slice(0,3000)||null,created_by:profile.id}).select(assignmentSelect).single();if(error?.code==="23505")throw new ApiError("This employee is already assigned to this module.",409);if(error)throw error;return Response.json({data:mapAssignment(data)},{status:201});}catch(error){return apiFailure(error);}}
export async function PATCH(request){try{const{client,profile}=await requireSession(request,["employee","manager","admin"]);const body=await request.json();if(!body.id||!statuses.includes(body.status))throw new ApiError("Assignment and valid status are required.");let error;if(profile.role==="employee"){({error}=await client.rpc("update_my_assignment",{p_assignment_id:body.id,p_status:body.status,p_checklist_progress:Array.isArray(body.checklistProgress)?body.checklistProgress:null}));}else{const update={status:body.status,updated_at:new Date().toISOString()};if(Array.isArray(body.checklistProgress))update.checklist_progress=body.checklistProgress;if(body.status==="Completed")update.completed_at=new Date().toISOString();if(body.status==="In Progress")update.started_at=new Date().toISOString();({error}=await client.from("work_assignments").update(update).eq("id",body.id));}if(error)throw error;const{data,error:readError}=await client.from("work_assignments").select(assignmentSelect).eq("id",body.id).single();if(readError)throw readError;return Response.json({data:mapAssignment(data)});}catch(error){return apiFailure(error);}}
