import { ApiError, apiFailure, requireSession } from "@/lib/supabaseServer";
export const dynamic="force-dynamic";
const map=row=>({id:row.id,employeeId:row.employee_id,employee:row.profiles?.full_name||"Employee",latitude:row.latitude,longitude:row.longitude,message:row.message,resolvedAt:row.resolved_at,createdAt:row.created_at});
export async function GET(request){try{const{client}=await requireSession(request,["manager","admin"]);const{data,error}=await client.from("sos_alerts").select("*,profiles!sos_alerts_employee_id_fkey(full_name)").is("resolved_at",null).order("created_at",{ascending:false});if(error)throw error;return Response.json({data:data.map(map)});}catch(error){return apiFailure(error);}}
export async function POST(request){try{const{client,profile}=await requireSession(request,["employee"]);const body=await request.json();const lat=Number(body.location?.latitude),lng=Number(body.location?.longitude);if(!Number.isFinite(lat)||!Number.isFinite(lng))throw new ApiError("A current GPS location is required for SOS.");const{data,error}=await client.from("sos_alerts").insert({employee_id:profile.id,latitude:lat,longitude:lng,message:String(body.message||"Emergency assistance requested").slice(0,500)}).select("*,profiles!sos_alerts_employee_id_fkey(full_name)").single();if(error)throw error;return Response.json({data:map(data)},{status:201});}catch(error){return apiFailure(error);}}
export async function PATCH(request){try{const{client,profile}=await requireSession(request,["manager","admin"]);const body=await request.json();const{data,error}=await client.from("sos_alerts").update({resolved_at:new Date().toISOString(),resolved_by:profile.id}).eq("id",body.id).select("*,profiles!sos_alerts_employee_id_fkey(full_name)").single();if(error)throw error;return Response.json({data:map(data)});}catch(error){return apiFailure(error);}}
