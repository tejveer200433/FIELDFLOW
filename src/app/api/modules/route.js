import { ApiError,apiFailure,requireSession } from "@/lib/supabaseServer";
import { mapModule } from "@/lib/projectData";
const moduleSelect=`*,work_assignments(*,employee:profiles!work_assignments_employee_id_fkey(full_name,email),reviewer:profiles!work_assignments_reviewer_id_fkey(full_name,email),work_submissions(*,submission_files(*)))`;
export async function POST(request){try{const{client,profile}=await requireSession(request,["manager","admin"]);const body=await request.json();if(!body.projectId||!body.title?.trim())throw new ApiError("Project and module title are required.");const checklist=Array.isArray(body.checklist)?body.checklist.map(item=>String(item).trim()).filter(Boolean).slice(0,50):String(body.checklist||"").split("\n").map(item=>item.trim()).filter(Boolean).slice(0,50);const{data,error}=await client.from("project_modules").insert({project_id:body.projectId,title:String(body.title).trim().slice(0,180),description:String(body.description||"").slice(0,5000)||null,checklist,sort_order:Number(body.sortOrder)||0,created_by:profile.id}).select(moduleSelect).single();if(error)throw error;return Response.json({data:mapModule(data)},{status:201});}catch(error){return apiFailure(error);}}
